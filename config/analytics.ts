// GA4 is loaded via GTM (GTM-MB6KB9CS) only — direct gtag scripts removed to prevent double-counting.
export const analyticsConfig = {
  googleAnalytics: [] as { async?: boolean; src?: string; innerHTML?: string; type?: string }[]
}
